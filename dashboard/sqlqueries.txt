create database dashboard;

CREATE TABLE dashboard.User (
    id int NOT NULL,
    email varchar(90) NOT NULL,
    pwd varchar(75) NOT NULL,
    PRIMARY KEY (email) 
);

#https://spring.io/guides/gs/accessing-data-mysql/#_create_the_application_properties_file
#grant all on dashboard.* to 'whom'@'%'; -- Gives all privileges to the new user on the newly created database